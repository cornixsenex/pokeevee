mapscripts GatoCity_MapScripts {

    MAP_SCRIPT_ON_TRANSITION {
        
        //Clear Truck Scene
        if (flag(FLAG_PECCADUM_TRUCK_READY)) {
            clearflag (FLAG_PECCADUM_TRUCK_READY)
        }

    }

}
